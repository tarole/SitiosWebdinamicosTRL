<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Producto</title>
    <link rel="stylesheet" href="/css/stylesM.css">
</head>

<body>
    <div class="form-container">
        <h2>Editar Producto</h2>
        <form action="/productos/editar/<%= producto.id %>" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="nombres">Nombre:</label>
                <input type="text" name="nombre" value="<%= producto.nombre %>" required>
            </div>
            <div class="form-group">
                <label for="nombres">Precio:</label>
                <input type="number" name="precio" step="0.01" value="<%= producto.precio %>" required>
            </div>
            <div class="form-group">
                <label for="nombres">Detalle:</label>
                <textarea name="detalle" required><%= producto.detalle %></textarea>
            </div>
            <div class="form-group">
                <label for="imagen">Imagen Actual:</label>
                <% if (producto.imagen) { %>
                    <br><img src="/img/<%= producto.imagen %>" width="100"><br>
                    <% } %>
            </div>
            <div class="form-group">
                <label for="imagen">Nueva Imagen:</label>
                <input type="file" name="imagen" accept="image/jpeg,image/png,image/gif">
            </div>
            <div class="form-group">
                <label for="imagen2">Imagen 2 Actual:</label>
                <% if (producto.imagen2) { %>
                    <br><img src="/img/<%= producto.imagen2 %>" width="100"><br>
                    <% } %>
            </div>
            <div class="form-group">
                <label for="imagen2">Nueva Imagen 2:</label>
                <input type="file" name="imagen2" accept="image/jpeg,image/png,image/gif">
            </div>
            <button type="submit" class="submit-btn">💾 Guardar</button>
        </form>
        <br>
        <a href="/productos" class="no-link">⬅️ Volver a lista</a>
    </div>
</body>

</html>